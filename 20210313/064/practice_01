//費式數列遞迴
#include <stdio.h>
#include <stdlib.h>


int fib (int i)
{
    if (i == 0 || i == 1)
    return i;
    return (fib (i - 1) + fib (i - 2));
}

int main()
{
   int n;
   scanf("%d", &n);
   printf("fib(%d) = %d\n", n, fib(n));
   return 0;
}
